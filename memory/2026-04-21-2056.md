LiteLLM ingress issue diagnostics and fixes

- Litellm pod in openclaw namespace is failing with Prisma/PostgreSQL connectivity and API-key auth errors
- Ingress host litellm.deeps-home.duckdns.org is reachable, but health endpoint returns 401 Unauthorized due to missing API key in requests
- Pod currently in CrashLoopBackOff, repeating health checks
- Potential root causes:
  - DATABASE_URL misconfig or DB unreachable
  - No API key provided in requests (Litellm expects API key header)
  - Prisma schema/db migrations failing due to env var or DB URL format
- Next steps (I will execute if you approve):
  1) Fix environment to ensure API key is passed (litellm-secrets contains API key) or bypass if testing
  2) Validate DATABASE_URL is correct: postgresql://litellm:litellm123@litellm-postgres:5432/litellm
  3) Inspect DB pod service litellm-postgres
  4) Patch deployment to reflect correct DB URL and test health
